Silex needs the following to run:
<br/>
<br/>
PHP version 5 or more
sessions
set_include_path
URL rewriting is not strictly necessary but you will not be indexed by robots without it.
MySQL is also not strictly necessary but you won't be able to run some of the Silex addons without it.

<br>